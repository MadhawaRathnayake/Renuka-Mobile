import 'package:flutter/material.dart';

class Featured_Tours extends StatelessWidget {
  const Featured_Tours({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}